[project]
name = "chatllm"
version = "0.1.0"
description = "CLI for local Ollama models"
dependencies = ["click", "rich"]

[project.scripts]
chatllm = "chatllm.cli:cli"

[tool.setuptools]
packages = ["chatllm"]

